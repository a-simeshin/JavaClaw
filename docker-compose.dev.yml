services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: javaclaw
      POSTGRES_USER: javaclaw
      POSTGRES_PASSWORD: javaclaw
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
